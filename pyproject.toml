[project]
name = "cape-roof-geometry"
version = "1.0.0"
requires-python = ">=3.12"
license = { text = "GPL-3.0-only" }

[project.optional-dependencies]
calibration = ["pypdf==6.10.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 110
target-version = "py312"
